import "../../styles/sidebar/sidebar.css";
import SearchBar from "./SearchBar";

const SideBar = () => {
    return (
        <div id="sidebar">
            <SearchBar />
        </div>
    );
};

export default SideBar;
